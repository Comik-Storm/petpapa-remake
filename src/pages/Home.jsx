import React from 'react';

import {Text, View} from 'react-native';
import SafeView from "../components/SafeView";

const Home = () => {
    return (
        <SafeView>
            <Text>Hi!</Text>
        </SafeView>
    );
};

export default Home;
